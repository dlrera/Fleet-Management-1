from django.apps import AppConfig


class FleetMaintenanceConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.fleet_maintenance'
    verbose_name = 'Fleet Maintenance'
